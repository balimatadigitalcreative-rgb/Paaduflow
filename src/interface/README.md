# Interface

Titik masuk dari luar: endpoint API, komponen UI React, dan CLI.

Seluruh nilai visual berasal dari `tokens.css` yang dibangkitkan dari `docs/tokens.json`. Lint akan menolak nilai warna atau spacing yang ditulis langsung.

Ditentukan di Sesi A1 · lihat `docs/Build_Playbook_Claude_Code.md`
